#login
  - if @current_user
    %span.welcome Hi, @#{@current_user.nickname}!
    %br
    = link_to 'Log Out', logout_path, method: "delete"
  - else
    = link_to 'Login via Twitter', '/auth/twitter'